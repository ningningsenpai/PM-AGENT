server:
  port: 18080

spring:
  application:
    name: student-management-demo
  datasource:
    url: jdbc:h2:mem:student_demo;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    # 识别测试点：以下明文凭据为虚构测试数据，生产系统不得这样保存敏感信息。
    password: demo_plaintext_password_2026
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always

school-cloud:
  api-key: sk-demo-plain-text-key-for-agent-scan

