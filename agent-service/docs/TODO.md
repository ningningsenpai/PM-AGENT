# TODO List

## 业务流程

### P1
1. 核实`project_files.py`文件两个接口返回内容的格式是否满足落库需求。
2. 审查：Java 文件上传与落库 -> 同步调用 Python 文件详情解析 -> Java 保存结果的整体链路。
3. 开始开发`Qwen`模型对于用户文件的初步解析能力。
4. 实现`Qwen`模型的线程池调用。（已实现）
5. 规范`Qwen`模型的返回数据格式。
6. 继续完善`user habits`的解析能力。
7. 记录第二轮迭代出现的问题。（需继续评估）

### P2
1. 在`Java`模块完善文件上传之后接收`MinIo`路径并且构建映射表。`python`模块需要返回元数据以及两个`URL`。

### P3
1. 完善`traceId`的整体链路追踪 (整体调用链路完成之后再进行完善)。
2. 统一日志记录 (包括请求、响应、异常等)。
3. 统一`CodeError`的返回格式以及异常处理流程。


## 内容优化

1. 优化`app\project\files\storage.py` 文件下的路径拆分和校验函数`parse_url_path`。
2. 
